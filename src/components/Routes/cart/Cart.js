import React from "react";
import CartModal from "./CartModal";
const Cart = (props) => {
  // const CartHandler = () => {
  //   if (props.shown === true) {
  //     props.setShown(false);
  //   }
  // };
  return (
    <div>
      <CartModal />
    </div>
  );
};

export default Cart;
